#include<stdio.h>#include<stdlib.h>#define N 5#define Len sizeof(float)void main(){	float *p,*q;	int i;	p = (float *)malloc(N*Len);	q = p;	p = (float *)realloc(p,(N+3)*Len);	//使用realloc后，内存地址不变	printf("p=0x%x\n",p);	printf("q=0x%x\n",q);	printf("Len=%d\n",Len);	for(i=0;i<N+3;i++){		p[i] = i;		printf("p[%d]=%4.1f p[%d]=%4.1f\n", i, p[i], i, q[i]);	}		p = (float *)malloc(10);	q = p;	p = (float *)realloc(p,1000);	printf("p=0x%x\n",p);	printf("q=0x%x\n",q);}