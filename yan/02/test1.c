#include<stdio.h>#include<stdlib.h>#define Len sizeof(int)#define N 5void main(){	int *p = malloc(Len);//申请整数指针	float *array = (float*)malloc(N*sizeof(float)),*pm;	int **arr;	*p = 13;	int i,j;	printf("p = %d, p+1 = %f\n",*p,*(p+1));	printf("%d ",p);	//free(p);	//free(p);	printf("%d ",p);	printf("\nmalloc one-dimensional array:\n");	for(i=0;i<N;i++){		array[i] = i+1;		printf("%4.2f\t",array[i]);	}	pm = &array[N-3];	printf("pm = %4.1f\n",*(pm+1));	free(array);//释放第一维指针		//申请二维数组	printf("\nmalloc two-dimensional array:\n");	arr = (int**)malloc(N*sizeof(int*));//第一维	for(i=0;i<N;i++){		arr[i] = (int*)malloc((*p)*sizeof(int));//第二维		for(j=0;j<(*p);j++){			arr[i][j] = i+j+1;			printf("%-4d", arr[i][j]);		}		printf("\n");	}	//先释放行指针	for(i=0;i<N;i++){		free(arr[i]);//释放第二维指针	}	free(arr);	printf("%ld %f",arr,arr);}