#include<stdio.h>void main(){	int year,month,day;	int speyear(int);	int spemonth(int);	int getday(int,int);	int days;	printf("Please enter year,month,day:\n");	scanf("%d %d %d",&year,&month,&day);	days = getday(year,month)+day;	printf("Today is the %d of %d",days,year);}//check the second month has 28 daysint speyear(int year){	if((year%4==0&&year%100!=0)||(year%400==0)){		return 29;	}else{		return 28;	}}//How many days in this month.int spemonth(int month){	int mday[7] = {1,3,5,7,8,10,12};//has 31 days	int i;	for(i=0;i<7;i++){		if(mday[i]==month) return 31;	}	return 30;}int getday(int year,int month){	int i,sum=0;	for(i=1;i<month;i++){		if(i==2){			sum += speyear(year);		}else sum += spemonth(i);	}	return sum;}