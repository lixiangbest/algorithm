#include<stdio.h>#define Max(x,y) (x>y)?x:yvoid main(){	int x,y,z;	int ch;	int max(int,int);	printf("Enter the x,y,z:\n");	scanf("%d %d %d",&x,&y,&z);	ch = Max(x,y);	if(ch<=z){		printf("use define to get max = %d",z);	}else{		printf("use define to get max = %d",ch);	}	printf("\n");	ch = max(x,y);	if(ch<=z){		printf("use function to get max = %d",z);	}else{		printf("use function to get max = %d",ch);	}}int max(int x,int y){	return (x>y)?x:y;}